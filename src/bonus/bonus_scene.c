#include "mini_rt.h"

void	bonus_scene(t_scene *scene)
{
	s1_sphere(scene);
	s2_sphere(scene);
	s3_sphere(scene);
	p1_plane(scene);
	p2_plane(scene);
	p3_plane(scene);
	c1_cylinder(scene);
	c1_cube(scene);
}
